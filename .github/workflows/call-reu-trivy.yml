name: Caller Workflow Trivy

on:
  workflow_dispatch:


jobs:
  call-reusable-workflow:
    uses: ./.github/workflows/reu-trivy.yml
    secrets: inherit  # Passes ALL secrets from the caller workflow to the reusable workflow
